# OpenClaw Knowledgebase Configuration
#
# Env cascade: workspace .env (parent dir) loads first, then this file
# overrides. Shared vars (SUPABASE_URL, API keys) can live in the
# workspace .env so all projects inherit them.
#
# Override the workspace path: OPENCLAW_WORKSPACE=/path/to/workspace

# Supabase (required — or inherit from workspace .env)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=your-service-role-key-here
TABLE_PREFIX=kb

# ── Embedding Provider ──────────────────────────────────────────────
# Available: ollama, google, openai, custom
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_DIMENSIONS=768
EMBEDDING_TIMEOUT=120

# Ollama (when EMBEDDING_PROVIDER=ollama)
OLLAMA_URL=http://localhost:11434

# Google AI (when EMBEDDING_PROVIDER=google)
# Models: text-embedding-004 (768 dims default, configurable)
# GOOGLE_API_KEY=AIza...
# EMBEDDING_MODEL=text-embedding-004
# EMBEDDING_DIMENSIONS=768

# OpenAI (when EMBEDDING_PROVIDER=openai)
# Models: text-embedding-3-small (1536), text-embedding-3-large (3072)
# OPENAI_API_KEY=sk-...
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSIONS=1536

# Custom OpenAI-compatible API (when EMBEDDING_PROVIDER=custom)
# Works with vLLM, LiteLLM, LocalAI, etc.
# OPENAI_API_KEY=your-key
# OPENAI_BASE_URL=http://localhost:8000/v1
# EMBEDDING_MODEL=your-model-name

# ── Chunking ────────────────────────────────────────────────────────
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# ── Search ──────────────────────────────────────────────────────────
DEFAULT_MATCH_COUNT=10
SIMILARITY_THRESHOLD=0.5
SEMANTIC_WEIGHT=0.7

# ── OpenClaw Memory Module (optional) ──────────────────────────────
# Leave empty for legacy single-tenant mode.
# Run: python3 skills/bootstrap/bootstrap.py all
OPENCLAW_AGENT_NAME=
OPENCLAW_AGENT_KEY=
